import React from 'react';
import ReactDOM from 'react-dom';
import SzopinskiCalendar from './SzopinskiCalendar';

ReactDOM.render(
	<React.StrictMode>
		<SzopinskiCalendar />
	</React.StrictMode>,
	document.getElementById('root')
);
